h2 Add a New Dog!
.row
  .col-xs-4.col-xs-offset-1
    form
      .form-group
        label(for='name') Name
        input.form-control#name(type='text', name='name', placeholder='Rex', ng-model='dog.name')
      .form-group
        label(for='age') Age
        input.form-control#age(type='number', name='about', ng-model='dog.age')
      .form-group
        label(for='sex') Sex
        select.form-control#sex(type='text', name='sex', ng-model='dog.sex')
          option Male
          option Female
      .form-group
        label(for='photo') Photo
        input.form-control#photo(type='text', name='photo', ng-model='dog.photo')
      .form-group
        button.btn.form-control.btn-success(type='submit', ng-click='add(dog)') Add Dog
